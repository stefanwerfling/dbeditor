/*
 * Build-time constants injected via Vite's `define`. See vite.config.ts
 * for the substitution source. These appear as literals in the emitted
 * bundle; nothing reads them at runtime.
 */
declare const __APP_VERSION__: string;